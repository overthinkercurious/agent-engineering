# Knowledge index

<!-- agent-engineering:start -->
<!-- agent-engineering:snapshot:0fa789fe7a2313269cd127516cc88d177eb50d148ce25df51ac4f78cbdf9b7ab -->
<!-- agent-engineering:kit:1.1.2 -->
<!-- Generated by agent-engineering 1.1.2 from .dev/context/analysis.json
     input sha256 d8d059888a472c9935ab5354f311dc9e7eee6189c5a0ac175240ef72bcd75ef8
     analysed 2026-09-22T03:28:06.909Z; commit 50f92f3b1d9edaf8d0289dfdf55359ca82ac9419; dirty true -->

_Which document answers which question, and who reads it. Every other skill
in this kit reads this file first, instead of re-reading the repository._

## Documents

| Document | Answers | Read by |
|---|---|---|
| `stack.md` | What is this built with, what may I import, what is the resolved version | every role |
| `architecture.md` | Components, entrypoints, who owns what, dependency direction | architect, builder |
| `schema.md` | What persisted state looks like, migration path, mutable vs append-only | data-focused work |
| `commands.md` | Exact command, cwd, environment names, what it actually checks | builder, verifier |
| `decisions.md` | Why it is built this way; what was rejected and why | architect |

## Tagging contract

Every claim inside a managed block carries exactly one tag:

- `OBSERVED` - with the `path:line` the pass actually opened
- `INFERRED` - with the reasoning stated, not just the conclusion
- `UNKNOWN` - with the file or check that would resolve it

There is no fourth tag. `ASSUMED` does not exist in this kit.

## Reading coverage

| Measure | Value |
|---|---|
| Files parsed | 95 (100%) |
| Code files | 20 |
| Proposed for model reading | 20 (100%) |
| Fan-in weight covered | 100% |
| Route detection | unknown |
| Risk files covered | UNKNOWN |

_A proposed reading set is a plan, not proof anything was read._

## Limits of this snapshot

- Import edges come from regex, not a parser. Dynamic and computed imports are missed, so fan-in is a lower bound.
- Token estimates are bytes/4 and are approximate.
- Test coverage here is a filename-mention heuristic, not a coverage report.
- Selected-file coverage describes a proposed reading set, not a completed audit. No findings are validated by this sensor.
- Route discovery is convention-based and incomplete. Zero detected routes means unknown, not complete coverage.
- Unclassified imports remain unknown, not external. Fan-in measures only resolved edges.
- CI parsing supports ordinary GitHub Actions mappings and literal run blocks. Other forms need source review.

## Readiness

<!-- agent-engineering:judgment:index-readiness:start -->
**Stage 3 shape: combined**, one sequential pass in a single session. The sensor dump reports 2 components and ~75 parsed files — below the fan-out threshold — and the executing host's row is `dispatch: none` in `.dev/context/host.json`, so isolation was neither available nor bought. Independence of the four domain passes therefore **does not apply to this run** and is not claimed.

**This survey was taken after a remediation pass**, so it describes the repository as it now stands rather than as the 2026-09-22 architecture review found it. Three defects that review reported in these generated tables are fixed and no longer visible here: manifest and component rows rendered `UNKNOWN` (sensor/renderer field-name drift), and a phantom `GET /foo` route was attributed to the analyzer's own comment. `decisions.md` entries 7–13 record what changed and why.

**Files read (OBSERVED):** `README.md`, `AGENTS.md`, `docs/DESIGN.md`, `package.json`, `kit-version.txt`, both plugin manifests, `hooks/hooks.json`, `skills/ae-forge/SKILL.md`, `skills/ae-surveyor/SKILL.md`, `references/team.md`, `references/lenses/_index.md`, `references/team.json` (structure plus `risk`, `tiers`, `lenses`, `acceptance`, `domain_detectors`), `skills/ae-forge/scripts/forge.mjs` (declarations plus `start`, `list`, `note`, `phase`, `lenses`, `approve`, `finish`, `contract`, `runs`, `allowedPhases`, `TRANSITIONS`, `SECTIONS`), `skills/ae-forge/scripts/lens-select.mjs` (full), `skills/ae-forge/scripts/guard.mjs` (full), `skills/ae-surveyor/scripts/sense.mjs` (full), `artifact-support.mjs` (full), `rules.mjs` (full), `analyze.mjs` (declarations, route and enforcement sections), `verify-citations.mjs` (lines 1-60), `scripts/validate-forge.mjs` (full), `scripts/test-all.mjs` (full), `scripts/test-installer.mjs` (lines 1-60, 86), `references/stages/knowledge.md` (full), `evals/README.md` (lines 1-60), `evals/cases.json` (3 of 10 cases), plus `git status`, `git ls-files`, `git check-ignore`, and full `npm test` runs before and after the remediation.

**High-signal files NOT read:** the 20 lens files and 11 role files individually (only `_index.md` and the `team.json` signal lists); `forge.mjs` lines 940-1300 (`report`, `audit`, `cancel`); `knowledge.mjs` beyond the rendering lines cited; `scripts/validate-suite.sh`, `test-forge.mjs`, `test-artifacts.sh`, `eval-depth.mjs`; `references/targets.yml` beyond the dispatch rows; `references/stages/rules.md`; the six stage skills' bodies beyond `ae-plan` (full) and the resolve blocks of the others.

**Readiness by document:**

| Document | Status | Gap |
|---|---|---|
| `stack.md` | Ready | No `engines` pin exists to read; minimum Node stays UNKNOWN |
| `architecture.md` | Ready | Role and lens *contents* not read — ownership is taken from `team.json` and `_index.md` |
| `schema.md` | Ready | Correct precisely because there is no persistent state; the target-project stores are the useful part |
| `commands.md` | Ready | What each check asserts internally is summarised from its output for the older checks; the two new ones were read in full |
| `decisions.md` | Ready | Two entries are deliberately UNKNOWN (absent CI, unchecked version duplication) |

**Overall: READY FOR REUSE.** Every `path:line` citation resolves under `verify-citations.mjs`, which now runs inside `npm test`.

A generated knowledge base is not production certification of the code it describes.
<!-- agent-engineering:judgment:index-readiness:end -->
<!-- agent-engineering:end -->

## Notes

<!-- Human notes survive regeneration. Revalidate them when source changes. -->

