# agent-engineering:start
schema: 1
generated_at: "2026-09-13T00:00:00.000Z"
source_revision: "1111111111111111111111111111111111111111"
required_commands:
  - kind: test
    command: "node --test hidden/precedence.test.mjs"
required_evidence:
  - focused_regression
  - independent_probe
evidence_rules:
  bind_to_candidate_revision: true
  model_pass_is_advisory: true
  heuristics_must_be_labeled: true
judgment:
  required_user_journeys: "direct and wrapper callers"
  non_functional_budgets: "none for the synthetic fixture"
# agent-engineering:end
