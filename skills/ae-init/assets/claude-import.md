@AGENTS.md

<!-- The line above imports AGENTS.md. Claude Code reads CLAUDE.md and does not
     read AGENTS.md on its own, so this import is what keeps one set of project
     instructions instead of two that drift. Add Claude-specific instructions
     below this managed block, never inside it. -->
